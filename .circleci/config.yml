jobs:
  build:
    docker:
      - image: cimg/python:3.8
    parallelism: 1
    steps:
      - checkout
      - run:
          name: apoppo
          command: |
            chmod u+x stak
            ./stak
